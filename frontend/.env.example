# Example environment vars for the frontend (copy to .env.local or set in Vercel UI)
# Point this to your deployed backend origin (no trailing slash)
VITE_API_BASE_URL=https://your-backend-domain.example
